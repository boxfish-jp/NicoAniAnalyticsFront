type dbVideoType = {
  video_id: number;
  vaddtime: Date;
  ch_id: number;
  ch_seq: number;
  ch_seq_id: number;
  ch_seq_url: string;
  ch_seq_title: string;
  ch_seq_desc: string;
  ch_seq_thumb: string;
};
export default dbVideoType;
